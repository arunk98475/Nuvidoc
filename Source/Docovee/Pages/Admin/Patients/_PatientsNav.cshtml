@{
    var path = Context.Request.Path.Value ?? "";
    var onReminders = path.StartsWith("/Admin/Patients/Reminders", StringComparison.OrdinalIgnoreCase);
}
<nav class="admin-subnav" aria-label="Patients section">
    <a class="@(onReminders ? null : "active")" href="/Admin/Patients">Patient list</a>
    <a class="@(onReminders ? "active" : null)" href="/Admin/Patients/Reminders">Booking reminders</a>
</nav>
