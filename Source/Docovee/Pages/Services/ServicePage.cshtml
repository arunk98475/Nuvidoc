@page "/services/{slug}"
@model Docovee.Pages.Services.ServicePageModel
@{
    Layout = null;
    var p = Model.Post!;
    ViewData["Title"] = $"{p.Title} — {Branding.SiteName}";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <partial name="Shared/_Favicon" />
    @if (!string.IsNullOrWhiteSpace(p.MetaDescription))
    {
        <meta name="description" content="@p.MetaDescription" />
    }
    <link rel="stylesheet" href="~/css/docovee.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/cms-public.css" asp-append-version="true" />
</head>
<body>
@{ ViewData["ShowServicesLink"] = true; }
<partial name="Shared/_PublicNav" />

<div class="cms-shell cms-article-shell">
    <div class="cms-article-viewbox">
        <article class="cms-article">
            <header class="cms-article-header">
                <div class="cms-breadcrumb"><a href="/services">Services</a> <span>›</span> <span>@p.Title</span></div>
                <h1 class="cms-article-title">@p.Title</h1>
            </header>

            @if (!string.IsNullOrWhiteSpace(p.ImageUrl))
            {
                <img src="@p.ImageUrl" alt="@p.Title" class="cms-article-hero-img" />
            }

            @if (!string.IsNullOrWhiteSpace(p.VideoEmbedUrl))
            {
                var videoSrc = Docovee.BLL.Services.VideoEmbedHelper.ToEmbedUrl(p.VideoEmbedUrl) ?? p.VideoEmbedUrl;
                <div class="cms-video-wrap">
                    <iframe src="@videoSrc" title="@p.Title" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen loading="lazy"></iframe>
                </div>
            }

            <div class="cms-article-body">
                @Html.Raw(p.BodyHtml ?? "")
            </div>
        </article>
    </div>
</div>
@await Component.InvokeAsync("SiteFooter")
</body>
</html>
