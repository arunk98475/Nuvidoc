@page
@model Docovee.Pages.Account.Profile.EditModel
@{
    Layout = null;
    ViewData["Title"] = $"Edit Profile — {Branding.SiteName}";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/docovee.css" asp-append-version="true" />
</head>
<body>
<nav>
    <partial name="Shared/_SiteLogo" />
    <div class="nav-right">
        <a href="/Account/Profile" class="nav-link">My Profile</a>
        <a href="/Account/Logout" class="nav-link">Logout</a>
    </div>
</nav>

<div class="auth-page">
    <div class="auth-card auth-card-wide">
        <h1>Edit your profile</h1>
        <p class="auth-subtitle">Update your account details.</p>

        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert-error" role="alert">@Model.ErrorMessage</div>
        }

        <form method="post">
            <div class="form-group">
                <label>Username</label>
                <input value="@Model.Input.Username" class="field-input" disabled />
                <p class="hint">Username cannot be changed.</p>
            </div>
            <div class="form-group">
                <label asp-for="Input.FullName">Full name</label>
                <input asp-for="Input.FullName" class="field-input" />
            </div>
            <div class="form-group">
                <label asp-for="Input.DateOfBirth">Date of birth</label>
                <input asp-for="Input.DateOfBirth" type="date" class="field-input" />
            </div>
            <div class="form-group">
                <label asp-for="Input.Phone">Phone number</label>
                <input asp-for="Input.Phone" type="tel" class="field-input" />
            </div>
            <div class="form-group">
                <label asp-for="Input.NewPassword">New password</label>
                <input asp-for="Input.NewPassword" type="password" class="field-input" autocomplete="new-password" />
                <p class="hint">Leave blank to keep your current password.</p>
            </div>
            <button type="submit" class="btn-primary" style="width:100%;">Save Changes</button>
            <p class="auth-footer-link"><a href="/Account/Profile">Cancel</a></p>
        </form>
    </div>
</div>
</body>
</html>
